services:
  - type: web
    name: geo-weather
    runtime: docker
    dockerfilePath: ./Dockerfile.deploy
    healthCheckPath: /api/weather-impact/health
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
      - key: ENVIRONMENT
        value: "production"
